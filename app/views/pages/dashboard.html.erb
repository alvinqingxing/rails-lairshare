<h1>Dashboards#dashboard</h1>

<%= render "lairs/new", user: @current_user, lair: @lair %>

<%= render "lairs/edit", user: @current_user, lair: @lair %>

<%= link_to "Delete #{@lair.name}",
            lair_path(@lair),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
